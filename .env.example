NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_KEY=

# CRM inbound webhook (LeadConnector/GHL) — leads are forwarded here after saving.
LEAD_WEBHOOK_URL=

# Optional analytics — leave blank to disable. Each tool loads only if its ID is set.
NEXT_PUBLIC_GTM_ID=
NEXT_PUBLIC_GA_ID=
NEXT_PUBLIC_META_PIXEL_ID=
